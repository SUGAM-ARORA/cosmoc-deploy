apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cosmoc-deploy.fullname" . }}-test-connection"
  labels:
    {{- include "cosmoc-deploy.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cosmoc-deploy.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
